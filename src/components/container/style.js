import { StyleSheet } from 'react-native'
import { theme } from '../../utilities/theme'

export const styles = StyleSheet.create({
	cont: {
		flex: 1,
		alignItems: 'flex-start',
		paddingTop: theme.sizes.offset,
		backgroundColor: theme.colors.sacramentoGreen,
	},
})
